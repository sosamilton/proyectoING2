
{% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
    {% if is_granted("ROLE_ADMIN") %}
    <li class=""><a href="admin">Panel de Administracion</a></li>
    </ul>
    {% endif %}
    <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">{{ app.user.username}} <b class="caret"></b></a>
          <ul class="dropdown-menu ancho">
            <li><a href="{{ path('fos_user_security_logout') }}">Salir</a></li>
            <li><a href="#">Ver Perfil</a></li>
          </ul>
          
        </li>
    </ul>
{% else %}
    <ul class="nav navbar-nav navbar-right">
        <li class=""><a href="{{ path('login')}}">Entrar</a></li>
    </ul>
{% endif %}
